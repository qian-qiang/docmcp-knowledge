{
  "name": "reguverse-hub-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute reguverse-hub --remote --file=schema.sql",
    "db:migrate:billboard": "wrangler d1 execute reguverse-hub --remote --file=migrations/0001_billboard.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.80.0",
    "typescript": "^5.6.0"
  }
}
